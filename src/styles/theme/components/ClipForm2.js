import styled from "styled-components";

export default styled.div`
    background: ${props => props.theme.clipForm2.backgroundColor};
    clip-path: circle(41% at 61% 0);
    right: -50%;
    width: 100%;
    position: relative;
    border-radius: 10px;
    height: 100%;
`